# brand_assets/

Referenced by `AGENTS.md` ("Always check the `brand_assets/` folder before designing").

Drop project brand materials here — logos, color guides, style guides, reference images.
If assets exist here, agents use them instead of placeholders. Currently empty: no brand
assets survived from the original machine (this folder was never committed to the repo).
